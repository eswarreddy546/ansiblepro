- name: read variables file
  hosts: local
  connection: local
  become : yes
  readfiles:
   - course.yaml
  
  tasks:
   - name : printing after task
     ansible.builtin.debug:
      msg : "{{course}}"
    

   

