- name: install and enable nginx
  hosts: frontend
  tasks:
  - name: install
    ansible.builtin.package:


- name : install 
  ansible.builtin.package:
    name: nginx
    state: present

- name : enable
  ansible.builtin.package:
    name: nginx
    state: present
    enabled: yes